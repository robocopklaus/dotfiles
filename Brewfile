tap "homebrew/command-not-found"  # Backs the zsh command-not-found plugin

brew "chezmoi"             # Drives the entire bootstrap
brew "git"                 # Version control
brew "gh"                  # GitHub CLI — issues and this repository's map live there
brew "jq"                  # Bootstrap dependency: scripts parse JSON
brew "mas"                 # Bootstrap dependency: installs the App Store entries
brew "dockutil"            # Bootstrap dependency: reconciles the Dock
brew "mise"                # Runtime manager
brew "uv"                  # uvx one-shot runner for Python CLIs — not the Python runtime manager, which is mise
brew "antidote"            # zsh plugin manager, loaded by every shell
brew "oh-my-posh"          # Prompt, rendered by every shell
brew "zoxide"              # z — directory jumping
brew "cloudflare-cli4"     # Personal infrastructure
brew "gogcli"              # Google Suite CLI — Gmail, Calendar, Drive, Docs, Sheets
brew "homeassistant-cli"   # Home automation

cask "1password"                 # Password manager; root of the bootstrap trust chain
cask "1password-cli"             # op — renders the work identity; trust chain
cask "claude"                    # Primary coding agent — the desktop application; the CLI has no cask
cask "visual-studio-code"        # Editor
cask "ghostty"                   # Terminal
cask "google-chrome"             # Browser
cask "slack"                     # Messaging
cask "raycast"                   # Launcher
cask "mimestream"                # Mail
cask "obsidian"                  # Notes
cask "clockify"                  # Time tracking
cask "chatgpt"                   # Assistant
cask "whatsapp"                  # Messaging
cask "codex"                     # Coding agent
cask "docker-desktop"            # Containers
cask "finicky"                   # Routes URLs between browsers; background app, so no launch record by design
cask "font-meslo-lg-nerd-font"   # Bootstrap dependency: the glyphs oh-my-posh renders

mas "Pages", id: 361309726                     # Documents; no Homebrew cask exists
mas "Numbers", id: 361304891                   # Spreadsheets; no Homebrew cask exists
mas "GCal for Google Calendar", id: 1107163858 # Calendar; no Homebrew cask exists
mas "1Password for Safari", id: 1569813296     # Safari extension; no cask, and no launch record by design
